 const colors = ['red', 'blue', 'green', 'yellow', 'orange']

 const rev_colors = []

 for (let col = colors.length - 1; col >= 0; col--) {
     rev_colors.push(colors[col])

 }
 console.log(rev_colors);
